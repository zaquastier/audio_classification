https://www.youtube.com/watch?v=ZLIPkmmDJAc&ab_channel=NicholasRenotte

use tf.io.read_file, audio decode wav etc to run on gpu? 15:14
work with mono

or just convert numpy array to tensor

reduce sr to 16k


datasets: 21:41
pos = tf.data.Dataset.list_files(POS + '\*.wav')

question: if we open just 1.5 seconds, can we have (multiple) silence files?
wel

he does this:
42:32

6.1 creat a tensorflow data pipeline

data=data.map(preprocess)
data=data.cache()
data=data.shuffle(buffer_size=1000)
data=data.batch(16)	
data=data.prefetch(8)

Why?

arch: conv conv flatten dense dense

he wants to add maxpool to reduce parameters

4 epochs lol, no cross validation, do that at the end



https://www.youtube.com/watch?v=JzkiuPwGPM8&ab_channel=AIDemistified

mfccs
he mounts drive to access tgz
creates extracted features array, then csv with two columns, one with array of mfcss and the other is class
has to be heavy though